/**
 * Mock data — DEPRECATED
 *
 * Previously this file exported fake calls, campaigns, agents, charts, etc.
 * Pages have been migrated to real DB-backed data with proper empty states
 * (see EmptyChart component). This file is kept as an empty stub so any
 * stale import doesn't crash the build; the exports themselves are gone.
 *
 * If you find yourself adding fake data back here, instead:
 *   1. Build the real API endpoint
 *   2. Render an EmptyChart while there's no data
 *
 * Safe to delete this file once you've confirmed no remaining imports.
 */

export {};
