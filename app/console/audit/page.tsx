import ConsolePlaceholder from '../ConsolePlaceholder';

export default function ConsoleAuditPage() {
  return (
    <ConsolePlaceholder
      title="Security & Audit"
      subtitle="Platform-wide audit log"
      description="Every login, role change, billing action, and data export across all tenants. Required for ISO 27001 compliance and customer security questionnaires."
    />
  );
}
