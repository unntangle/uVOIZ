import ConsolePlaceholder from '../ConsolePlaceholder';

export default function ConsoleBillingPage() {
  return (
    <ConsolePlaceholder
      title="Platform Billing"
      subtitle="MRR, payments, refunds across all tenants"
      description="Aggregate revenue view, Razorpay payment events, refund management, failed payment recovery, and invoice generation across every BPO."
    />
  );
}
