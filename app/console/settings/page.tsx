import ConsolePlaceholder from '../ConsolePlaceholder';

export default function ConsoleSettingsPage() {
  return (
    <ConsolePlaceholder
      title="Platform Settings"
      subtitle="Pricing plans, feature flags, default configs"
      description="Edit the Starter/Pro/Agency pricing, configure feature flags per tenant, set platform-wide defaults like supported languages and TRAI compliance toggles."
    />
  );
}
