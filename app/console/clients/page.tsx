import ConsolePlaceholder from '../ConsolePlaceholder';

export default function ConsoleClientsPage() {
  return (
    <ConsolePlaceholder
      title="BPO Clients"
      subtitle="Manage every BPO company on the platform"
      description="Drill down into each tenant: usage, plan, credit balance, billing status, suspend/activate, and impersonate for support."
    />
  );
}
